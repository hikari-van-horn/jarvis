#!/usr/bin/env bash
# scripts/release.sh

echo "Releasing the package..."
# NOTE: Add your real releasing steps here. 
# Depending on requirements, it could be `poetry publish` or deploying to a server.
# For example: 
# poetry publish --build
echo "Release complete."
